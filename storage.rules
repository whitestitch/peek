rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to upload and read all files.
    // This is a safe and necessary rule for development.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}