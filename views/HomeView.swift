import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("🏠 You're back at the Home Screen")
                .font(.title2)
                .padding()
        }
    }
}
